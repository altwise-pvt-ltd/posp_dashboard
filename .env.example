
VITE_API_BASE_URL=https://ibmsapi.shrisoft.co.in/api

# The notification / content service behind the Marketing Kit. A different host
# and a different credential from VITE_API_BASE_URL — see
# src/shared/api/notificationClient.js.
#
# The base URL stops at the origin: every path in ENDPOINTS.notification carries
# its own /api prefix.
VITE_NOTIFICATION_BASE_URL=https://notification.shrisoft.co.in

# ⚠ Inlined into the JS bundle at build time and readable by anyone who opens
# devtools. Only safe while it is a read-only content key.
VITE_NOTIFICATION_API_KEY=rs2XYouEwBKEfFnZUQMbGI1
